# Review xpc-002-1 — port identity

Verified against [../tasks/xpc-002.md](../tasks/xpc-002.md) §Verification.

## Gate checks

| Check | Result |
|---|---|
| `yarn typecheck` | pass |
| `yarn build` | pass |
| No `registry.set` reachable from `__xpc_subscribe__` | pass — the three `registry.set` sites are `registerMainHandler` (`xpcCenter.helper.ts:54`), `registerPortHandler` (`:89`), and the renderer `__xpc_register__` listener (`:260`). The subscribe branch (`xpcMain.helper.ts:158`) calls only `addSubscriber` |
| portId minted at most once per `MessageChannelMain` | pass — `registerPort` returns the existing id when the port is already in `portIdByPort`, and `createUtilityProcess` calls it once at `xpcMain.helper.ts:116` before `port2.start()` |
| `__xpc_broadcast__` has no registration-dependent early return | pass — the `if (senderPortId)` guard is gone; `portId` is a closure constant |

## Findings

| # | Severity | Blocking | Finding |
|---|---|---|---|
| 1 | P3 | non-blocking | `registerPortHandler`'s second parameter changed from `MessagePortMain` to `string`, and `registerPort` is new. `xpcCenter` is exported from `electron-xpc/main`, so this is a breaking change to an exported (though internal-by-intent) method. Already scheduled for the changelog in [../tasks/xpc-006.md](../tasks/xpc-006.md) §Breaking-change note |
| 2 | P3 | non-blocking | `XPC_REGISTER` handling gained a `&& handleName` guard, matching the other three branches. Defensive but consistent with the file |

## Conclusion

**pass** — functional cases T5–T9 and T12 remain to be exercised by the xpc-005 harness, as the task
states.
