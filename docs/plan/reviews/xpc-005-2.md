# Review xpc-005-2 — functional gate

The gate left open by [xpc-005-1](xpc-005-1.md): `yarn test:app` → **Run all**, run by Ral on
2026-08-20 at 15:05 local. Every static check had already passed; this records the runtime evidence
for R1–R4, which no earlier task was allowed to claim.

## Result

**12/12 PASS.** RESULTS strip `T1 ✓ … T12 ✓`, log `run all finished — 12/12 passed`.

| Case | Observed | Proves |
|---|---|---|
| T1 | `main answered the utility` — `ret = {"from":"main","echoed":{"from":"utility-A"}}` | R1 utility → main |
| T2 | `renderer answered the utility` | R1 utility → renderer |
| T3 | `B answered A across two ports` — `ret = {"from":"B",…}`, not A | R1 utility → utility, genuinely cross-process |
| T4 | `self-send round-tripped on one port` — `ret = {"from":"A",…}` | R1 utility → itself |
| T5 | `xpcMain.send reached the utility` | R2 main → utility |
| T6 | `xpcRenderer.send reached the utility` | R2 renderer → utility |
| T7 | `main + renderer + B got it, A excluded` | R3/R4 utility broadcast + sender self-exclusion |
| T8 | `utilities + renderer got it, main excluded` | R3 main broadcast now reaches utilities |
| T9 | `main + utilities got it, renderer excluded` | R3 renderer broadcast now reaches utilities |
| T10 | `resolved null in 1.5ms` | unknown handleName does not park the caller |
| T11 | `dead → null in 13.1ms, A unaffected` | xpc-004 exit cleanup is scoped — killing B did not disturb A |
| T12 | `a utility owning zero handlers broadcast successfully` | a handler-less process is still a broadcast peer |

Self-exclusion is the strongest of these: T7/T8/T9 each name both the receivers **and** the one
process that must not receive, so a naive "send to everyone" implementation fails all three.

## Not a defect

Re-clicking **T3** after **Run all** logs `T3 FAIL — utility B was killed by T11 — restart the app
to run T3`. This is the single-shot guard from finding #3 in xpc-005-1: utility B can only be killed
once per launch, so T3 refuses to run rather than reporting a misleading assertion failure. The `Run
all` sequence itself is ordered T1→T12, so T3 runs while B is alive — which is why it passed above.
T11 and T12 carry the same guard.

## Conclusion

**pass.** xpc-005 → `done`. R1–R6 all have evidence; xpc-006 is unblocked and may now document
observed behavior.
